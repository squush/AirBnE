<%= simple_form_for [@crime, @booking] do |f| %>
  <%= f.input :target %>
  <%# change to calendar / look up 'flatpcikr' %>
  <%= f.input :date %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit, class:"btn btn-success" %>
<% end %>
